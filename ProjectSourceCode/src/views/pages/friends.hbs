<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Friends</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <link rel="stylesheet" href="../css/style.css">
</head>

<body style="align-items: center">
     <header class="mb-4">
        
        <h1>Game Friends Hub</h1>
        <p>Add friends to compare scores and compete!</p>
    </header>
    <div class="friends-list">
        <h2>Friends List</h2>
        <ul id="friendsList">
            {{#each friends}}
                <li>
                    <strong>{{this.username}}</strong>
                    <span>Best Score: {{this.bestScore}}</span>
                    <span>Category: {{this.category}}</span>
                </li>
            {{/each}}
        </ul>
    </div>
    
     
        <section class="actions">
            <h3>Manage Your Friends</h3>
            <form action="/friends/add" method="POST" class="form-inline mb-2">
                <input type="text" name="friendId" placeholder="Enter User ID" class="form-control mr-sm-2">
                <button type="submit" class="btn btn-primary">Add Friend</button>
            </form>

            <form action="/friends/remove" method="POST" class="form-inline">
                <input type="text" name="friendId" placeholder="Friend's User ID" class="form-control mr-sm-2">
                <button type="submit" class="btn btn-danger">Remove Friend</button>
            </form>
        </section>
        
        <section class="top-scores mt-4">
            <h3>Top Scores</h3>
            <p>Check out the high scores among your friends!</p>
            <button class="btn btn-info">View Scores</button> 
        </section>
    </main>
</body>
</html>